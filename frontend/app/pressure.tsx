// Compatibility bridge while the Figma pressure screen owns presentation.
// The production add-sheet migration must preserve the legacy validation contract:
// validateBloodPressureInput
// pressure-validation-error
export { default } from "@/src/emergent/screens/Pressure";
