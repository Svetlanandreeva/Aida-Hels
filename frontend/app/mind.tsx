// Compatibility bridge while the Figma mind screen owns presentation.
// Production data contracts retained for migration validation:
// activeProfile?.mental_conditions
// api.listMeds(activeId)
export { default } from "@/src/emergent/screens/Mind";
