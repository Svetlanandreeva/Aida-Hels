name: Final user testing patch once

on:
  push:
    branches:
      - feature/wearables-foundation
    paths:
      - scripts/final_user_testing_patch.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/wearables-foundation
      - name: Apply patch
        run: python scripts/final_user_testing_patch.py
      - name: Commit patch
        run: |
          rm -f scripts/final_user_testing_patch.py .github/workflows/final-user-testing-patch-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Finish privacy-aware reminders and production recovery links"
          git push origin HEAD:feature/wearables-foundation
